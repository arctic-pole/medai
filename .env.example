# Copy to .env and fill in real values. Never commit .env.

# --- Database ---
DATABASE_URL=postgresql+asyncpg://medai:medai@localhost:5432/medai
POSTGRES_USER=medai
POSTGRES_PASSWORD=medai
POSTGRES_DB=medai

# --- Auth ---
JWT_SECRET_KEY=change-me-in-real-environments-min-32-bytes-long
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=15
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# Fernet key for application-level encryption of sensitive DB columns.
# Generate a real one with: from cryptography.fernet import Fernet; Fernet.generate_key()
FIELD_ENCRYPTION_KEY=kgcgyGwg_TOkwTOdpCClu340NKpRIVU15x2EeNDDtqg=

# --- App ---
ENVIRONMENT=development
LOG_LEVEL=INFO

# --- Provider credentials (placeholders — no vendor chosen yet, see IMPLEMENTATION_PLAN.md) ---
# LLMProvider: OpenAI (decided Phase 3). Leave blank to run with extraction disabled
# (app/providers/llm/openai_provider.py fails closed rather than fabricating output).
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# STT_PROVIDER=
# STT_API_KEY=
# TTS_PROVIDER=
# TTS_API_KEY=
# EMBEDDING_PROVIDER=
# EMBEDDING_API_KEY=
# MEDICAL_KNOWLEDGE_PROVIDER=
# MEDICAL_KNOWLEDGE_API_KEY=
